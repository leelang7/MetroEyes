# --- Core DL ---
# PyTorch는 CUDA 버전에 맞춰 별도 설치 권장:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
torch>=2.2
torchvision>=0.17
numpy>=1.26
scipy>=1.12

# --- CV ---
opencv-python>=4.9
pillow>=10.2
einops>=0.7
ultralytics>=8.2          # YOLO11-pose (실시간 BEV 백엔드)

# --- BEV / 3D 보조 ---
pyquaternion>=0.9
shapely>=2.0

# --- Data / EDA ---
pandas>=2.2
pyarrow>=15.0
matplotlib>=3.8
seaborn>=0.13
plotly>=5.19
jupyter>=1.0
ipykernel>=6.29
jupytext>=1.16

# --- 서울 열린데이터 / HTTP ---
requests>=2.31
httpx>=0.27
aiohttp>=3.9
tenacity>=8.2

# --- API 백엔드 ---
fastapi>=0.110
uvicorn[standard]>=0.29
websockets>=12.0
pydantic>=2.6
pydantic-settings>=2.2

# --- LLM 인사이트 모듈 ---
anthropic>=0.40

# --- 실험 관리 ---
hydra-core>=1.3
omegaconf>=2.3
tensorboard>=2.16
tqdm>=4.66

# --- Lint / Test ---
ruff>=0.4
pytest>=8.1
pytest-asyncio>=0.23

# --- 환경변수 ---
python-dotenv>=1.0
